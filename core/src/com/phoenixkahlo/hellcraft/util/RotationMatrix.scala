package com.phoenixkahlo.hellcraft.util

/*
case class RotationMatrix(
                         a: Float, b: Float, c: Float,
                         d: Float, e: Float, f: Float,
                         g: Float, h: Float, i: Float
                         ) {

  def *(v: V3F): V3F =
    v match {
      case V3F(x, y, z) => V3F(
        a * x + b * y + c * z,
        d * x + e * y + f * z,
        g * x + h * y + i * z
      )
    }

}

object RotationMatrix {

  def between(v1: V3F, v2: V3F) = {

  }

}
*/